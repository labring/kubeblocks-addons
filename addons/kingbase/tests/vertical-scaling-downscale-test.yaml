# Run only after vertical-scaling-test.yaml has Succeeded and the HA checks pass.
# Disposable test only. Use a new metadata.name for each OpsRequest execution.
apiVersion: apps.kubeblocks.io/v1alpha1
kind: OpsRequest
metadata:
  name: kingbase-test-vscale-down-b16
  namespace: kingbase-test
spec:
  clusterRef: kingbase-ha-test
  type: VerticalScaling
  verticalScaling:
    - componentName: kingbase
      requests:
        cpu: 250m
        memory: 1Gi
      limits:
        cpu: "1"
        memory: 2Gi
